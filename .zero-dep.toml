track = "F"
pitch = "llamini.cpp: a from-scratch C99 llama.cpp core (hand-rolled GGUF parser, 8 quant formats, GQA/MQA transformer, SentencePiece + byte-level BPE tokenizers) that auto-detects and runs 4 real architectures -- LLaMA, Qwen2, GPT-2, Gemma -- and beats real llama.cpp's memory footprint on 3 of them, on libc and POSIX alone."
